<script>
  // Home
</script>

<p>
  Human-facing site. Agents get token-optimized JSON at the same URLs (e.g.
  <code>/docs/core</code>) when they send an LLM User-Agent or
  <code>Accept: application/vnd.llm+json</code>. Production routing is in
  <code>hooks.server.ts</code>.
</p>

<script>
  // Home page
</script>

<p>
  Human-facing site. Agents get token-optimized content at the same URLs (e.g.
  <code>/docs/core</code>) when they send an LLM User-Agent or
  <code>Accept: application/vnd.llm+json</code>. In production this is handled by
  <code>hooks.server.ts</code>.
</p>

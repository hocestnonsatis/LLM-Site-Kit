---
priority: high
category: getting-started
token_cost: 80
requires: []
---

# Setup

Install dependencies and start the dev server:

```bash
npm install
npm run dev
```

Then open http://localhost:5173. For agent response, use:

```bash
curl -A "GPTBot/1.0" http://localhost:5173/docs/setup
```
